spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fxdealdb
    username: fx
    password: fxpass

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

logging:
  level:
    root: INFO
    com.example.fxdeals: DEBUG
